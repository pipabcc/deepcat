# 隐私说明

DeepCat 处理截图、选中文字、剪贴板、笔记、对话与用户配置。本说明按当前实现区分本地处理、联网功能和数据保存位置。

## 本地数据与配置

源码运行时，默认数据根目录为项目根目录；打包运行时为 `deepcat.exe` 所在目录。主要数据包括：

- `settings.json` 及其备份：模型地址、API 密钥、代理、界面和功能设置。
- `data/`：复制记录、表格笔记、稍后阅读、待办、对话历史、提示词和相关图片等。
- `files/` 或自定义输出目录：截图、导出和附件。
- `models/`、`tools/` 及资源缓存：用户准备或下载的模型、工具和图标。

这些数据默认以 JSON、SQLite 或普通文件保存，不能视作经过整库加密的保密存储。界面中的密码保护也不等于文件加密，应结合操作系统账号和目录访问权限保护设备上的数据。

## 本地处理

框选与滚动截图、标注、贴图、图像导出、本地记录检索等主要在本机完成。RapidOCR 与 PP-OCRv6 Small 在模型齐备后使用本地推理；AI 识别属于另一条路径，可能发送图片到所选模型服务。

内置 Gemini 的应用内请求通过进程内通道分发，不建立 `8081` 的 TCP 监听。这只描述软件内部通信；Gemini 的上游网页请求仍然联网。

## 剪贴板与键鼠事件

复制记录默认启用，会读取剪贴板变化并保存支持的文本、图片或路径。关闭记录开关不会立即清空已有历史；已有内容仍可能按独立的自动清理策略处理，也可在相关页面或数据管理中手动管理。

截图、识别或问答结果可能按设置写入系统剪贴板。剪贴板是本机共享资源，其他有权限的软件也可能读取它。

全局快捷键、划词和截图操作需要处理相应键鼠事件。划词取词还可能临时模拟复制并恢复剪贴板。事件用于本地交互，选中的内容只有在相应功能请求时才会交给所选服务。

## 可能联网的功能

| 功能 | 可能发送或接收的内容 |
| --- | --- |
| 在线翻译、问答、解释、总结等 | 输入文本、所选上下文、提示词及服务所需参数。 |
| AI 识别、带图问答 | 选定图片或附件，以及对应提示词。 |
| 模型测试与模型列表 | 连接参数、鉴权信息及测试请求；内置 Gemini 的本地就绪测试不等于一次上游生成请求。 |
| Gemini / ChatGPT 网页适配 | 向上游网页服务发送请求，并使用适配所需的会话或账号信息。 |
| 模型下载、更新检查、网络探测 | 请求下载、版本或探测地址；远端会接收到普通网络请求信息。 |
| 资源图标、链接和代码预览 | 可能加载目标网页、图标、脚本或代码指定的网络资源。 |
| WebDAV、其他已配置同步 | 所选备份、笔记或同步数据发送到配置的服务器。 |

实际目的地址由模型、服务和同步配置决定。第三方服务对内容的处理适用其自身政策；使用前应确认所选内容允许发送给该服务。

## 外部本地 API

“模型管理”中的“本地API”是供其他工具访问的真实 HTTP 服务，默认使用回环地址和 `11888` 端口。将其改为可供其他设备访问时，会改变数据暴露范围，应配置合适的访问凭据。

默认测试密钥是公开示例值，不能当作个人秘密。服务的网络自检或“一键允许”不改变模型服务商的数据处理方式，详见 [本地 API 文档](LOCAL_API_SERVICE.md)。

## 日志、备份与公开反馈

应用日志和崩溃日志默认关闭。开启后，日志可能包含本地路径、请求元信息、错误、输入或输出摘要等。命令行和独立服务也可能输出诊断信息，重定向到文件时同样需要检查。程序会对部分密钥、URL 参数和鉴权字段做脱敏，但并不保证任意业务文本均已移除。

提交问题时只提供必要片段，先检查截图、日志、配置和附件中的个人内容。`.gitignore` 只影响未跟踪文件，不能撤销已经提交到 Git 历史的敏感数据。

备份可能包含配置中的凭据和历史内容。启用远程备份前应检查包含项及目标服务器，删除本地记录也不会自动删除已经导出的文件或远端备份。
