# DeepCat 使用指南

本文按当前界面说明日常操作。源码安装和构建过程分别见 [README](README.md) 与 [BUILDING](BUILDING.md)。

## 安装、启动与升级

打包版需要完整解压后运行 `deepcat.exe`，同级的 `_internal` 是程序依赖目录，不能只复制一个 EXE。源码运行使用：

```powershell
.\.venv\Scripts\python.exe main.py --gui
```

主窗口提供“复制记录、表格记事、稍后阅读、模型管理、实用工具、截图设置、数据管理、关于”等入口。左下角四个快捷按钮依次是开始截图、截图目录、AI 对话和设置。

关闭主窗口通常会隐藏到系统托盘；从托盘菜单选择“退出”才会结束程序。再次打开程序时，单实例机制会尝试激活已有窗口。

升级前先退出程序，并通过“数据管理”备份。程序文件与个人数据的处理方式如下：

| 内容 | 升级时的处理 |
| --- | --- |
| `deepcat.exe`、`_internal/` | 使用同一版本的成套文件；替换整个旧运行库目录，避免混入旧 DLL。 |
| `settings.json`、`settings.json.bak`、`data/` | 保留原配置、记录和数据库；不要用测试目录覆盖。 |
| `files/`、自定义输出目录 | 保留截图、导出和附件文件。 |
| `models/`、`tools/` | 保留自行安装的模型和推理工具，并确认与所选模型匹配。 |

## 截图与长图

### 框选截图

1. 在“截图设置”选择保存位置、格式和框选后动作。
2. 按 `F1`，拖动鼠标框选区域。
3. 使用截图后的工具条标注、保存、复制、贴图或识别文字。

工具条支持线框、箭头、画笔、文本、序号等标注。连续截图可以按设置处理上一张截图，例如置顶、保存、拼接或暂存。

### 滚动截图

按 `F2` 选择目标滚动区域，按界面提示启动捕获。捕获过程中可暂停、继续或结束，`Esc` 用于停止相应操作。页面动画、吸顶元素、复杂嵌套滚动区等可能影响拼接，应检查最终图像。

长图拼接块可在达到内存预算后暂存到磁盘。PNG 采用分块编码，但最终保存的是一张完整图像；超过 JPEG 尺寸限制时会改为完整 PNG。图像显示和后续编辑仍需要内存，遇到内存不足时应先保留已经输出的原图。

### 录制和 GIF

录制及相关导出入口提供帧序列处理和 GIF 输出。GIF 受调色板、时长和帧数影响，长时间或大分辨率内容会增加生成时间与文件大小。是否保留原始尺寸、帧率等以当前导出设置为准。

## 文字识别

截图后的“文字识别”菜单提供：

| 选项 | 处理方式 |
| --- | --- |
| `OCR识别` | RapidOCR，在本机推理。 |
| `PP-OCRv6识别` | PP-OCRv6 Small 检测与识别模型，在本机通过 ONNX Runtime 推理。 |
| `AI识别` | 使用配置的视觉模型，可能发送图片到外部服务。 |

两套本地 OCR 使用隔离工作进程。工作进程可复用，默认空闲约 120 秒后回收；切换引擎时会替换相应进程。安装包自带 PP-OCRv6 固定模型，源码用户应先完成 Git LFS 拉取与模型校验。

OCR 结果取决于图片清晰度、文字尺寸、语言和版式。可以在两套本地引擎间比较结果；识别后调用在线翻译或问答，会产生独立的联网请求。

## 模型管理、问答与划词

### 配置模型

在“模型管理”中选择或新增模型，按服务要求填写模型类型、API 地址、模型名称、密钥和代理。翻译与问答可以分别选择不同模型，不必共用一个配置。

当前支持按供应商整理模型、测试配置、获取服务提供的模型列表，以及管理提示词。预置名称来自项目配置，具体可用性以所用服务和账号为准。

内置 Gemini 使用进程内请求通道，原有 `http://127.0.0.1:8081` 配置用于识别这一通道，应用内调用不再监听或连接这个端口。访问 Gemini 网页服务仍需可用网络。ChatGPT Web 和 HY-MT 的通信方式不同，见 [本地 API 文档](LOCAL_API_SERVICE.md)。

### 划词

在左下角齿轮打开“设置”，启用“划词开关”。选中文字后：

- `Ctrl+Space` 触发划词翻译。
- `Ctrl+B` 打开划词自选面板。
- 开启“划词浮窗”后，可通过选区旁的按钮操作；关闭该浮窗开关仍可使用已启用的划词快捷键。

取词通常需要目标程序支持复制选中文字。目标程序的权限、复制行为、输入框状态和快捷键占用都可能影响取词，应先在普通文本窗口中确认。

AI 对话支持流式输出、提示词、历史和附件等操作。模型是否支持图片、较长上下文或特定接口，取决于实际服务；不要仅根据模型名称推断能力。

ChatGPT Web 默认使用 `auto` 网页自动模式，并保留 GPT-5.5 Thinking、GPT-5.6 和 GPT-6 固定型号，具体权限以网页账号为准。回答会边生成边显示，上游补全引用或修正正文时会更新已有回答；断线续流重发的同一片段不会再次追加。模型与协议细节见 [ChatGPT Web 说明](CHATGPT_WEB.md)。

## 复制记录、记事与稍后阅读

### 复制记录

页面顶部开关控制记录功能，默认启用。可以按关键词和类型筛选、置顶、编辑、删除记录，或打开快捷浮窗。

鼠标悬停在搜索框右侧的统计区域时，会提示“双击该处空白位置打开批量管理”。双击后，每条记录前显示勾选框；可以逐条勾选，也可以点击记录标题切换选择。“全选”选择当前已加载的非置顶记录，选择数量会同步更新；点击“完成”退出并清空选择。

自动清理默认开启，当前默认策略为保留 90 天、最多 10000 条、记录数据容量阈值 1024 MB。置顶保护会影响实际保留总量；具体阈值由配置中的 `ui.clipboard_history` 管理，“过滤”按钮用于关键词过滤。记录数据容量统计不等同于 SQLite 文件的物理大小，重要内容应另行备份。

页面会预读最近记录和置顶项，在后台查询完成前保留现有内容。首次数据库尚未就绪时会显示读取状态；真正没有数据时显示空状态提示。

识别到 HTML、SVG 等可预览代码时，列表提供运行入口。该功能会在浏览器打开临时页面，页面中的 JavaScript 和网络资源可能执行或加载，请只运行可信内容。

### 表格记事

支持表格与笔记标签、分组、文本排版、Markdown 展示、图片和附件，以及表格汇总等操作。可通过相应入口将截图或识别结果加入笔记。

笔记页面的密码保护用于限制界面访问，不代表整个数据库或附件文件已加密。若启用外部同步功能，请确认目标账号和数据范围。

### 稍后阅读与搜索

`F3` 打开链接采集工具。将指示位置移到链接附近，按工具提示采集；可在“稍后阅读”整理和筛选。不同程序的链接与辅助功能支持可能影响识别。

搜索框右侧的统计区域也提供双击进入批量管理的悬停提示。没有记录时，统计栏显示 `0 / 0 条 | 0 KB`；仅因筛选而暂时没有匹配结果时，仍显示已有数据的大小。

侧边栏搜索用于查找本地内容。功能模块在“设置”中关闭后，相应入口和后台活动会按模块生命周期处理。

## 实用工具与数据管理

- 待办支持日程、提醒和相应的重复设置；休息提醒可配置间隔、时长和提示方式。
- 资源快捷方式可指向应用、目录或网址；获取网站图标、探测网络等操作会联网。
- 磁盘清理会展示扫描结果、大小和风险级别。确认具体路径、文件用途和备份后再执行清理，风险标签不能代替对个人文件的判断。
- “数据管理”提供备份、恢复和清理等操作。恢复会修改本地数据，建议先保存独立备份；WebDAV 启用后会把所选备份发送到配置的服务器。

## 快捷键与操作阶段

| 快捷键 | 作用和条件 |
| --- | --- |
| `F1` | 框选截图，可在“截图设置”修改。 |
| `F2` | 滚动截图，可在“截图设置”修改。 |
| `F3` | 稍后阅读采集，可在“截图设置”修改。 |
| `Alt+Space` | 智能问答窗口，可在“截图设置”修改。 |
| `Ctrl+Space` | 日常划词翻译；GUI 滚动捕获期间暂停或继续。 |
| `Ctrl+B` | 划词自选，需启用划词功能。 |
| `Alt+N` | 新建待办。 |
| `Alt+T` | 显示或隐藏活动贴图。 |
| `Alt+J` / `Alt+F` / `Alt+S` | 记事、复制记录、稍后阅读的快捷浮窗。 |
| `Esc` | 取消当前截图、捕获或相应浮窗操作。 |
| `Enter` / `Ctrl+Enter` / `Shift+Enter` | 对话输入中的问答、翻译、换行操作，依当前面板状态生效。 |

截图工具条还提供自身的快捷操作，以当前按钮提示为准。Windows 热键被其他功能或程序占用时会提示失败；配置未成功应用时，原有效绑定会保留。

## 数据保存位置

源码运行时，默认数据根目录是项目根目录；打包运行时，是 `deepcat.exe` 所在目录，**不是固定的 AppData 目录**。

| 路径 | 用途 |
| --- | --- |
| `settings.json`、`settings.json.bak` | 配置和配置备份，包括模型地址、密钥等。 |
| `data/clipboard_history.db`、`data/clipboard_images/` | 复制记录和受管理的剪贴板图片。 |
| `data/table_notes.db` | 表格与笔记数据。 |
| `data/later_read.db`、`data/todo.db` | 稍后阅读和待办。 |
| `data/translation_history.db`、`data/prompt.db` | 对话历史及提示词。 |
| `files/` 或自定义目录 | 截图和导出；附件与其他资源还可能使用单独目录。 |
| `logs/` | 启用日志后产生的诊断记录。 |

迁移时应备份整个应用数据范围及自定义输出目录，仅复制 SQLite 主文件可能遗漏 WAL 中尚未合并的变更或外部图片。优先使用应用的备份流程。

## 命令行

以下为源码入口示例，不等同于打包 GUI 的参数接口：

```powershell
.\.venv\Scripts\python.exe main.py --help
.\.venv\Scripts\python.exe main.py --region 100,100,800,600 --format png --output-dir .\files
```

CLI 滚动截图会提示开始，默认倒计时 3 秒、滚动步长 `-24`、滚动等待 `0.06` 秒；`--max-frames 0` 表示不按帧数设限，仍可手动终止。CLI 使用 `Ctrl+D` 暂停或继续，与 GUI 滚动捕获的 `Ctrl+Space` 不同。GUI 默认滚动等待值由 `Config.SCROLL_DELAY` 控制，当前为 `0.03` 秒。

## 常见问题

| 现象 | 排查方向 |
| --- | --- |
| 关闭窗口后程序仍在运行 | 检查系统托盘，并使用菜单中的“退出”。 |
| 快捷键没反应 | 检查功能开关、注册错误、其他程序占用，以及目标应用权限和复制支持。 |
| OCR 模型缺失或校验失败 | 重新获取完整发布包；源码运行时执行 Git LFS 拉取和模型校验。 |
| 长图拼接异常 | 检查目标滚动区、动态内容、固定头尾和实际输出文件。 |
| `QtCore` DLL 加载失败 | 使用成套发布文件，不混合不同版本的 `_internal`；构建者参见构建文档的运行库说明。 |
| 外部工具无法连接本地 API | 检查开关、实际地址、IPv4/IPv6、自检结果和网络策略，见本地 API 文档。 |
| 内置 Gemini 测试通过但生成失败 | 测试主要确认内部通道与模型路由；继续检查上游网页服务、账号、网络和代理。 |

更多信息见 [本地 API](LOCAL_API_SERVICE.md)、[本地 HY-MT 模型](HY_MT_LOCAL_TRANSLATION_GUIDE.md) 和 [隐私说明](PRIVACY.md)。
