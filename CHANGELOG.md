# 更新日志

当前代码版本声明为 `1.0.0`。下面的“未发布”记录当前源码相对初始功能基线的变化，不代表已经上传 GitHub Release。

## 未发布

### 新增与改进

- ChatGPT Web 增加 `gpt-image-2.5` 网页生图入口，支持图片接口及对话接口调用，并可独立指定 `picture_v2` 的上游对话模型。
- AI 回答中的网络图片异步显示为缩略图，点击可在应用内查看大图；复制与保存使用原始图片数据，加载失败可点击重试。
- ChatGPT Web 按登录配置与代理隔离复用连接，短时缓存已经验证的登录态，减少后续请求的准备开销；失败、取消或服务停止时释放相关连接。
- ChatGPT Web 在当前请求被接收后，提前完成下一轮校验；预取结果只使用一次，过期、配置切换或校验拒绝时回到正常验证流程。
- ChatGPT Web 增加明确的 `auto` 网页自动模式并设为默认，与 Edge 网页的成功请求方式一致；固定型号保留独立入口。
- ChatGPT Web 文本目录保留 GPT-5.5 Thinking，增加 GPT-5.6 和 GPT-6 配置入口，迁移本地旧型号并保留登录态与角色选择；实际权限以上游账号为准。
- 模型按供应商组织，支持独立选择翻译与问答模型，以及模型测试、列表获取、提示词和相关配置操作。
- 内置 Gemini 的应用内调用改为进程内 HTTP 语义通道，覆盖模型测试、列表和请求响应，不再监听 `127.0.0.1:8081`；旧地址保留为配置路由标识。
- 本地 API 网关增加 IPv6 回环监听和 IPv4/IPv6 实际可达性自检。打包版在相应失败场景提供由用户主动触发的授权入口。
- 长图拼接增加内存预算与磁盘暂存，PNG 支持逐块编码为完整图像；超出 JPEG 尺寸限制时回退为完整 PNG。
- 复制记录增加首屏只读快照、异步查询、增量统计和子串搜索索引；在后台刷新时保留已显示内容。
- OCR 工作进程可复用，并在空闲或切换引擎时回收；两套本地引擎共用隔离通道。
- HTTP 客户端复用、流式响应取消、上游工作线程资源管理和恢复事务等基础能力得到整理。

### 修复

- 修正网页搜图的来源文章被误当成图片、网络图片标记显示为普通链接的问题，兼容旧历史中来源文章与真实缩略图成对出现的记录。
- AI 输入框增加中文输入法预编辑和上屏状态保护，防止选词时回车发送、Esc 关闭或焦点重置打断输入；合并连续输入触发的附件整理与布局刷新。
- ChatGPT Web 流内错误保留上游的错误码与说明，不再把单独的“稍后重试”文案当作额度限制证据；额度提示引导核对账号、实际模型及思考档位。
- ChatGPT Web 默认按上游正文事件即时输出，补齐 SSE 结束标记，不再等待整段缓冲或最终轮询。
- 修正流式快照、引用补全和断线续流造成的整段重复；桌面客户端支持显式替换正文，WebSocket 按编号与载荷过滤重放，同时保留合法重复文字和 Markdown 格式。
- 普通全局快捷键使用 Windows 系统热键注册；注册冲突能报告错误，修改失败保留原绑定。
- 修正划词快捷键被自动取词抑制误挡、按住 Ctrl 时无法复制选区，以及漏收抬键后的恢复问题。
- 修正复制记录标题延迟省略造成的闪烁、首次切页空白，以及部分列表刷新和重复项问题。
- 修正复制记录使用缓存列表进入批量管理时勾选框缺失、全选失效，以及选择数量和按钮状态不同步的问题。
- 复制记录与稍后阅读统计区域增加双击进入批量管理的悬停提示，稍后阅读无记录时显示 `0 KB`。
- 重绘截图目录 SVG，并统一侧边栏快捷图标的绘制尺寸。
- 修正 Qt 下拉框代理样式共享导致的资源释放问题。
- 构建隔离原生 DLL 搜索路径，避免把其他工具的 ICU/UCRT 混入；VC 运行库优先采用 Qt 配套版本，并保留 DLL 搜索目录句柄。
- Gemini 进程内处理器的异常可反馈给调用方，不再仅以启动超时掩盖处理失败。
- 网页适配的直连请求可使用 Python DNS 解析结果交给 libcurl；使用 `socks5h` 时保留代理解析。这是连接处理改进，不代表绕过外部网络策略。
- 补充功能开关生命周期、剪贴板统计、数据恢复、清理路径和日志脱敏等场景的处理。
- 恢复模型菜单批量删除的候选过滤函数，修正缺失声明导致的异常。
- 修正新建 Windows 用户配置中缺少注册表 Run 项时无法开启自启动的问题。
- 修正历史侧栏、聊天气泡和记事本窗口销毁时的事件访问，延迟更新随所属控件销毁取消；界面配置保存复用窗口定时器，避免读取已删除的控件。

### 文档

- 更新界面入口、快捷键、配置与数据路径，以及源码和打包版的运行说明。
- 区分内置 Gemini、对外本地 API 和其他模型服务，纠正“超时必然是防火墙”“禁用规则等同于阻止”等描述。
- 整理构建、升级、成品验证、贡献和开源发布流程，移除个人机器部署结论及未经验证的性能保证。
- 更新依赖许可证快照，保留第三方许可证原文，并说明其环境范围。
- 整理 GitHub 首次公开源码快照，补充本地缓存与内部材料的忽略规则，修正 CI 首次提交的比较基准和测试配置隔离。

## 1.0.0 功能基线 — 2026-09-04

以下为项目原有功能基线记录，发布状态以实际 Release 为准：

- Windows 框选与滚动截图、标注、贴图和图像保存。
- RapidOCR 与 PP-OCRv6 Small 离线识别。
- 在线翻译、模型问答、划词浮窗和流式输出。
- 复制记录、表格笔记、稍后阅读、待办及休息提醒。
- 磁盘清理分析、资源快捷方式及本地 API 服务。
- GPL-3.0-or-later 授权、分层测试和 Windows CI 配置。
