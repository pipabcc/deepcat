# 开源与发布核对清单

本清单用于仓库公开或制作 Release 前的实际核对，不表示项目已经完成安全审计、远端发布或全部测试。

## 仓库与历史

- [ ] 确认目标 GitHub 仓库、公开范围、默认分支及 README 中的仓库链接。
- [ ] 检查当前跟踪文件，排除个人配置、密钥、Cookie、数据库、截图、日志、运行缓存和本机路径。
- [ ] 检查全部历史分支与提交。`.gitignore` 不能移除已跟踪或历史中的内容。
- [ ] 单独审查历史审计报告、临时实施计划、性能报告和资源图标缓存，决定哪些适合随源码公开。
- [ ] 如发现泄露的凭据，先撤销或轮换，再处理历史与公开材料。

基础检查示例：

```powershell
git status --short
git ls-files
git log --all -- settings.json settings.json.bak data logs files
git diff --check
```

文本检索可辅助核对，但不能代替对二进制附件、Git LFS 对象、历史和实际凭据的检查。

## 依赖、模型与许可证

- [ ] 按 `pyproject.toml` 与 requirements 入口准备干净构建环境，并运行 `pip check`。
- [ ] 拉取 Git LFS 对象并运行 PP-OCRv6 模型校验。
- [ ] 保留 `LICENSE`、`COPYRIGHT`、第三方声明及实际构建环境对应的许可证材料。
- [ ] 分别确认 Python 包、Qt/VC 运行库、可选 llama.cpp、模型权重和外部视觉资源的分发条件。
- [ ] 核对许可报告的生成时间和环境；许可证快照不是依赖锁文件，也不是自动合规结论。

## 代码、文档与验证

- [ ] 同步 README、使用指南、API 文档和更新日志，区分已实现能力、实验入口和计划。
- [ ] 验证命令、默认值、界面名称、路径、端点和相对链接。
- [ ] 执行与改动相关的静态检查、测试及成品验证，记录未覆盖的环境。
- [ ] 确认 Windows CI 的触发分支、Python 矩阵和比较基准适用于目标仓库。
- [ ] 不在发布说明中使用未经验证的性能、覆盖率或“绝不失败、无需任何授权”等承诺。

## 发布包

- [ ] 从干净、独立的输出目录制作发布包，不把开发目录恢复进去的个人配置和数据一起压缩。
- [ ] 完整分发 EXE 与对应的 `_internal`，不要混合旧运行库。
- [ ] 携带所需用户文档、版权和许可证文件，标明模型和可选工具是否包含。
- [ ] 按 [BUILDING.md](BUILDING.md) 检查启动、截图、OCR、数据、模型通道和服务生命周期。
- [ ] 附带 SHA-256、适用平台、升级与数据备份说明。
- [ ] 如提供代码签名，核对签名状态；签名不能保证所有系统或第三方策略均自动放行。

## 当前项目约定

运行数据、模型和构建输出的忽略规则以 [.gitignore](.gitignore) 为准；PP-OCRv6 的 LFS 规则见 [.gitattributes](.gitattributes)。其他模型和 `tools/` 默认需要另外准备。

仓库已配置 Windows CI，包含静态检查、多个 Python 版本的测试和 PyInstaller 产物检查。实际执行状态以目标提交的 CI 结果为准；本清单不预先勾选尚未执行的发布事项。
